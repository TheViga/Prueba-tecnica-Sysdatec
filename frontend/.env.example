# Optional. Where the browser sends API calls. Defaults to the same-origin "/api"
# path, which works out of the box both in Docker (nginx proxies it to the
# backend) and in local dev (Vite proxies it to http://localhost:3004).
# Only set this if you serve the backend somewhere else.
# VITE_API_URL=http://localhost:3004/api
